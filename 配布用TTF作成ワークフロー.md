# 配布用オリジナルTTFフォント 作成ワークフロー

**対象読者**：本ツール（漢字編集Webアプリ）を使ってオリジナルTTFフォントを配布したい人
**目的**：Word・PowerPointを含む、あらゆるアプリ・OSで確実にインストール＆適用できる配布用TTFを作る

---

## 全体の流れ

```
元のOTF
  ↓（FontForge）
ステップ1：ベースTTFを仕込む（フォント名・言語フラグを修正）
  ↓（自作HTMLツール）
ステップ2：漢字を編集してTTFを書き出す
  ↓
ステップ3：完成・配布
```

---

## 背景：なぜこの手順が必要なのか（トラブルシューティング事例）

以前、以下の症状が発生しました。

- Wordでは自作TTFフォントが問題なく使えた
- PowerPointではフォントの選択肢には表示されるが、選択しても文字に適用されなかった

### 原因

Noto Sans JP（CJK版）のように日中韓すべての文字を含む巨大なOTFフォントを、FontForgeでTTFに変換すると、FontForgeが自動判定で「これは中国語フォントだ」と誤認識し、`OS/2`テーブル内の言語コードページフラグを

- 日本語フラグ（932）→ OFF
- 繁体字中国語フラグ（950）→ ON

という誤った状態で書き出してしまうことがあります。

本ツール（自作HTMLエディタ）は `glyf` など必要なテーブルのみを差し替え、それ以外のテーブルは元のバイナリをそのまま温存する「サージカル・エクスポート（Surgical Export）」方式で動作しています。これは編集内容以外の情報を極力壊さないための優れた設計ですが、裏を返すと **入力（ベースTTF）に含まれる誤ったフラグもそのまま温存されてしまう** ということでもあります。

PowerPointは（Wordより）フォントのコードページフラグを厳密にチェックするため、この誤フラグが原因で「選べるが適用されない」という症状が発生していました。

### 対策の要点

> **本ツールで編集する「前」に、FontForgeで正しいメタデータ（フォント名・言語フラグ）を持つ「ベースTTF」を作っておく。**

これにより、本ツールのSurgical Exportの仕様を活かしたまま、正しいメタデータを持つ最終TTFを得られます。

---

## ステップ1：FontForgeで「ベースTTF」を仕込む

配布先のどのPC・どのアプリでもトラブルなく動作するよう、編集前のTTFの土台（メタデータ）を整えます。

### 1-1. OTFの読み込み

大元のOTF（例：Noto Sans JPなど）をFontForgeで開きます。

### 1-2. フォント名の完全な独立化

メニューから「エレメント」＞「フォント情報」を開きます。

- 「PS Names」タブと「TTF名」タブの両方で、配布用の独自フォント名（例：`Kyujitai-Regular`）を設定します。
- **注意**：「TTF名」タブの「スタイル（サブファミリー）」は必ず `Regular` のみにしてください。元のフォントのスタイル名（Bold、Light等）が残っていると、配布先での表示や適用に不具合が出ることがあります。

> 元のフォント名をそのまま残さず完全に独立させることで、元フォント（例：Noto Sans JP本体）と名前が衝突し、配布先の環境で混同・上書きされるのを防ぎます。

### 1-3. 日本語フラグ（MSコードページ）の修正

「エレメント」＞「フォント情報」＞「OS/2」＞「文字セット」タブを開きます。

画面右下の「コードページ」の一覧を確認します。

1. 一覧の中から **「932, 日本語」** を探してクリックし、選択状態（青色）にします。
2. もし **「950, 繁体字中国語」** など、日本語以外の東アジア言語フラグが誤って選択されている場合は、`Ctrl` キーを押しながらクリックして選択を解除します。
3. 最終的に「932, 日本語」だけが選択されている状態にします。

> ここが今回の不具合の根本原因でした。このフラグが誤っていると、Wordでは問題なくてもPowerPointでは「選べるが適用されない」という症状が起こります。

### 1-4. ベースTTFの書き出し

「ファイル」＞「フォントを出力」から、形式として **TrueType（TTF）** を選択して書き出します。

- 例：`Base_Kyujitai.ttf`
- このファイルは中身（グリフ）はまだ編集前ですが、名前と言語フラグは配布用に正しく整った状態になっています。

---

## ステップ2：自作HTMLツールで編集する

1. 自作のWebアプリ（例：`kyujitai_editor.html`）をブラウザで開きます。
2. ステップ1で作成した **`Base_Kyujitai.ttf`**（元のOTFではなく、こちらを使う点に注意）を読み込みます。
3. 通常どおり、旧字体などに漢字を編集します。
4. 「TTFを書き出す」ボタンで書き出します。

> 本ツールはSurgical Exportにより、`glyf`など編集対象のテーブルのみを差し替え、`name`テーブルや`OS/2`テーブル（ステップ1で設定したフォント名・言語フラグ）はベースTTFの内容がそのまま温存されます。

---

## ステップ3：完成・配布

書き出されたTTFファイルは、以下の情報を正しく引き継いでいます。

- ステップ1-2で設定した独自のフォント名
- ステップ1-3で設定した日本語フラグ（932）

このファイルであれば、配布先のユーザーが以下のどの環境でも、インストールするだけで確実にフォントが適用されます。

- Windows / Mac
- Word / PowerPoint

---

## チェックリスト（配布前の確認用）

- [ ] FontForgeで独自フォント名（PS Names / TTF名の両方）を設定したか
- [ ] TTF名のスタイル（サブファミリー）が `Regular` のみになっているか
- [ ] OS/2＞文字セットで「932, 日本語」のみが選択されているか（950などが混在していないか）
- [ ] 編集には元のOTFではなく、ステップ1で作った「ベースTTF」を使ったか
- [ ] 書き出したTTFをWord・PowerPoint両方で実機テストしたか

---

## 補足：本ツールの仕様について

本ツールは、フォント全体を作り直すのではなく、`glyf`テーブルなど編集に必要な部分のみを差し替え、それ以外のテーブル（`name`、`OS/2`、`cmap`など）は元のTTFのバイナリをそのまま保持する **Surgical Export** 方式を採用しています。

この仕様のメリットは、元フォントが持つメタデータや細かな設定を壊さずに編集できることですが、裏を返せば「入力するベースTTFの品質がそのまま出力の品質になる」という特性でもあります。そのため、本ワークフローのようにステップ1で入力側を整えておくことが、確実な配布物を作る鍵になります。
